-r requirements_base.txt

# onnxruntime-gpu must come from the CUDA 13 index — the standard PyPI build targets CUDA 12.x
# and the CUDA provider fails silently (segfault) on the CUDA 13 system here.
# Install with: pip install onnxruntime-gpu==1.26.0 \
#   --index-url https://aiinfra.pkgs.visualstudio.com/PublicPackages/_packaging/onnxruntime-cuda-13/pypi/simple/ \
#   --extra-index-url https://pypi.org/simple/
onnxruntime-gpu==1.26.0
transformers==4.38.0

# PyTorch with CUDA 12.8 — required for Blackwell GPUs (RTX 5070 Ti, sm_120)
# Install with: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.7.1+cu128
torchvision==0.22.1+cu128
torchaudio==2.7.1+cu128
