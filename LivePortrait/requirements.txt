-r requirements_base.txt

# onnxruntime-gpu 1.20.1 is the latest version that ships working CUDA shared libs via PyPI.
# 1.21+ wheels on PyPI have a broken onnxruntime namespace (empty module); use 1.20.1 until
# a fixed wheel is published.
onnxruntime-gpu==1.20.1
transformers==4.38.0

# PyTorch with CUDA 12.8 — required for Blackwell GPUs (RTX 5070 Ti, sm_120)
# Install with: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.7.1+cu128
torchvision==0.22.1+cu128
torchaudio==2.7.1+cu128
